---
phase: 01-aesthetic-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - components/ServiceCard.tsx
  - app/page.tsx
autonomous: true
requirements:
  - AES-01
  - AES-02
  - AES-03
  - SERV-01
  - FOOT-01

must_haves:
  truths:
    - "Hovering a service card shows a radial spotlight glow that follows the cursor position — the card feels physically illuminated"
    - "Service cards have a visible neon border glow on hover (in addition to the spotlight)"
    - "Each service card shows 2-4 capability bullets below the description"
    - "The footer Privacy link navigates to /privacy-policy (not a dead hash anchor)"
    - "The 'Digital Commerce' gradient heading and 'Command Center' / 'Ready to Upgrade?' headings have visible neon glow"
  artifacts:
    - path: "components/ServiceCard.tsx"
      provides: "ServiceCard with SpotlightCard cursor effect, neon glow, and bullets prop"
      contains: "useMotionTemplate"
    - path: "app/page.tsx"
      provides: "Page with bullets passed to ServiceCards, text glow on headings, fixed Privacy link"
      contains: "href=\"/privacy-policy\""
  key_links:
    - from: "components/ServiceCard.tsx"
      to: "mouseX, mouseY MotionValues"
      via: "onMouseMove handler calls mouseX.set() and mouseY.set()"
      pattern: "mouseX\\.set"
    - from: "components/ServiceCard.tsx"
      to: "spotlightBackground MotionValue"
      via: "useMotionTemplate composes radial-gradient from mouseX/mouseY"
      pattern: "useMotionTemplate"
    - from: "app/page.tsx"
      to: "components/ServiceCard.tsx"
      via: "bullets prop passed to each ServiceCard instance"
      pattern: "bullets=\\{"
---

<objective>
Upgrade ServiceCard with the SpotlightCard cursor-tracking glow effect, three-layer neon border glow, and capability bullets. Update page.tsx to pass bullet content to each ServiceCard, apply neon glow to headings, and fix the footer Privacy link.

Purpose: ServiceCard is the most visible interactive element on the page — the spotlight effect makes it feel premium and physically reactive. The capability bullets satisfy SERV-01 and give visitors concrete specifics. The footer link fix is a functional bug. This plan owns all page.tsx changes alongside the ServiceCard component.
Output: Updated ServiceCard.tsx (SpotlightCard + bullets prop), updated page.tsx (bullets, heading glows, privacy link).
</objective>

<execution_context>
@/Users/Farhan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Farhan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-aesthetic-foundation/01-RESEARCH.md
@.planning/phases/01-aesthetic-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ServiceCard.tsx — SpotlightCard effect + neon glow + bullets prop</name>
  <files>components/ServiceCard.tsx</files>
  <action>
    Rewrite ServiceCard.tsx to add:
    1. SpotlightCard cursor-tracking radial gradient (AES-03)
    2. Three-layer neon border glow on hover (AES-01)
    3. `bullets?: string[]` prop and rendering (SERV-01)

    Full implementation:

    ```tsx
    "use client";

    import { motion, useMotionTemplate, useMotionValue } from "framer-motion";
    import { ReactNode, MouseEvent } from "react";

    interface ServiceCardProps {
      icon: ReactNode;
      title: string;
      description: string;
      bullets?: string[];
      index: number;
      className?: string;
    }

    export default function ServiceCard({
      icon,
      title,
      description,
      bullets,
      index,
      className = "",
    }: ServiceCardProps) {
      const mouseX = useMotionValue(0);
      const mouseY = useMotionValue(0);

      function handleMouseMove({ currentTarget, clientX, clientY }: MouseEvent<HTMLDivElement>) {
        const { left, top } = currentTarget.getBoundingClientRect();
        mouseX.set(clientX - left);
        mouseY.set(clientY - top);
      }

      const spotlightBackground = useMotionTemplate`radial-gradient(500px circle at ${mouseX}px ${mouseY}px, rgba(14, 165, 233, 0.10), transparent 80%)`;

      return (
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, delay: index * 0.1 }}
          viewport={{ once: true }}
          className={`relative group h-full ${className}`}
          onMouseMove={handleMouseMove}
        >
          {/* Spotlight overlay — pointer-events-none so it doesn't block card interactions */}
          <motion.div
            className="pointer-events-none absolute -inset-px rounded-3xl opacity-0 transition-opacity duration-300 group-hover:opacity-100"
            style={{ background: spotlightBackground }}
          />

          <div className="relative h-full bg-glass border border-glass-border rounded-3xl p-8 transition-all duration-300 hover:border-primary/40 hover:neon-glow-cyan overflow-hidden">
            {/* Subtle gradient blob */}
            <div className="absolute -right-20 -top-20 w-64 h-64 bg-primary/5 rounded-full blur-3xl group-hover:bg-primary/10 transition-colors duration-500" />

            <div className="relative z-10 flex flex-col h-full">
              <div className="mb-6 p-4 rounded-2xl bg-white/5 w-fit border border-white/10 group-hover:border-primary/30 transition-colors duration-300">
                {icon}
              </div>
              <h3 className="text-2xl font-bold mb-4 text-white group-hover:text-primary transition-colors duration-300">
                {title}
              </h3>
              <p className="text-gray-400 leading-relaxed group-hover:text-gray-300 transition-colors duration-300">
                {description}
              </p>
              {bullets && bullets.length > 0 && (
                <ul className="mt-4 space-y-1.5">
                  {bullets.map((bullet, i) => (
                    <li key={i} className="text-sm text-gray-500 flex items-start gap-2 group-hover:text-gray-400 transition-colors duration-300">
                      <span className="text-primary mt-0.5 flex-shrink-0">›</span>
                      {bullet}
                    </li>
                  ))}
                </ul>
              )}
            </div>
          </div>
        </motion.div>
      );
    }
    ```

    Key decisions:
    - `whileHover={{ y: -5 }}` is removed — the spotlight effect gives enough hover feedback without the lift animation (which can feel jittery combined with spotlight tracking)
    - `hover:neon-glow-cyan` applies the three-layer box-shadow from globals.css on hover via CSS transition
    - `useMotionValue` + `useMotionTemplate` updates the radial gradient position WITHOUT triggering React re-renders — critical for 60fps mouse tracking
    - The spotlight `motion.div` uses `absolute -inset-px` to sit just inside the card border, and `opacity-0 group-hover:opacity-100` to show only on hover
    - Bullets are optional (`bullets?`) so existing card usage without bullets continues to work
  </action>
  <verify>
    Run `npm run dev`. Hover over each service card and move the cursor around — a glowing spotlight should follow the cursor position, making the card feel physically illuminated. The card border should also glow cyan on hover. Capability bullets should be visible below the description (once page.tsx is updated in Task 2).
  </verify>
  <done>
    ServiceCard.tsx uses useMotionValue + useMotionTemplate for SpotlightCard effect. Card border glows cyan on hover (neon-glow-cyan). Component accepts `bullets?: string[]` prop and renders a bullet list. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx — pass bullets to ServiceCards, glow headings, fix Privacy link</name>
  <files>app/page.tsx</files>
  <action>
    Update `app/page.tsx` with three changes:

    **Change 1: Add Next.js Link import and fix Privacy link (FOOT-01)**
    - Add `import Link from "next/link";` to imports at the top
    - In the footer, replace:
      `<a href="#" className="hover:text-white transition-colors">Privacy</a>`
      With:
      `<Link href="/privacy-policy" className="hover:text-white transition-colors">Privacy</Link>`
    - The existing page at `app/privacy-policy/page.tsx` maps to `/privacy-policy` in the URL. Use this exact path.
    - Do NOT change the Terms or Twitter links — only Privacy needs fixing per FOOT-01.

    **Change 2: Add text-neon-gradient to gradient heading, text-neon-white to solid headings (AES-02)**
    - The `<h1>` gradient span (line ~68 in original): `<span className="bg-gradient-to-r from-primary via-accent-cyan to-white bg-clip-text text-transparent">`
      Add `text-neon-gradient` to this span's className: `className="bg-gradient-to-r from-primary via-accent-cyan to-white bg-clip-text text-transparent text-neon-gradient"`
      WHY: `text-shadow` does not work on `text-transparent` elements. `text-neon-gradient` uses `filter: drop-shadow()` which works on transparent text.
    - The `<h2 className="text-4xl md:text-5xl font-bold mb-6">Command Center</h2>`:
      Add `text-neon-white` to its className.
    - The `<h2 className="text-5xl md:text-7xl font-bold mb-8">Ready to Upgrade?</h2>`:
      Add `text-neon-white` to its className.

    **Change 3: Pass capability bullets to each ServiceCard (SERV-01)**
    Update each ServiceCard JSX call to include a `bullets` prop with relevant capability descriptions:

    Service 1 — Custom Software Development (md:col-span-2 md:row-span-2):
    ```tsx
    bullets={[
      "SaaS platforms and internal tooling on Next.js / Node.js",
      "Real-time dashboards and admin panels",
      "Third-party integrations and webhook pipelines",
      "Cloud deployment on Vercel, AWS, or GCP",
    ]}
    ```

    Service 2 — Shopify Apps (md:col-span-1 md:row-span-1):
    ```tsx
    bullets={[
      "Custom storefront extensions and theme app blocks",
      "Checkout UI extensions and post-purchase flows",
      "Subscription and loyalty program integrations",
    ]}
    ```

    Service 3 — Automation (md:col-span-1 md:row-span-1):
    ```tsx
    bullets={[
      "n8n and Make.com workflow orchestration",
      "Order management and inventory sync pipelines",
      "Slack, email, and CRM notification automation",
    ]}
    ```

    Service 4 — Enterprise Integration & API Development (md:col-span-3 md:row-span-1):
    ```tsx
    bullets={[
      "REST and GraphQL API design and documentation",
      "ERP, CRM, and payment gateway connectors",
      "Real-time data sync across Shopify, HubSpot, and custom DBs",
    ]}
    ```

    These bullets are purposefully specific to give visitors confidence in actual capabilities. Flag for copy review — the patterns and technical specifics are directionally correct but content owner should confirm.
  </action>
  <verify>
    Run `npm run dev`.
    1. Verify privacy link: Scroll to footer, click "Privacy" — it should navigate to /privacy-policy page (not stay on # or 404).
    2. Verify heading glows: "Digital Commerce" gradient text should have a visible cyan glow. "Command Center" and "Ready to Upgrade?" should have white neon text-shadow.
    3. Verify bullets: Each service card should show capability bullets below the description text.
    Run `npm run build` — must pass with no TypeScript errors.
  </verify>
  <done>
    `app/page.tsx` imports Next.js Link. Footer Privacy link uses `<Link href="/privacy-policy">`. All four ServiceCard instances have bullets prop. Gradient heading has text-neon-gradient. Solid headings have text-neon-white. Build passes with no type errors.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- ServiceCard spotlight effect follows cursor (visible in browser)
- Service cards show capability bullets for all 4 services
- Footer Privacy link navigates to /privacy-policy
- Gradient h1 text has visible drop-shadow glow
- "Command Center" and "Ready to Upgrade?" headings have text-shadow glow
</verification>

<success_criteria>
ServiceCard has SpotlightCard cursor illumination and neon border glow. All 4 service cards display capability bullets. Footer Privacy link routes to /privacy-policy. Page headings have neon text glow appropriate for their type (gradient vs solid). Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-aesthetic-foundation/01-04-SUMMARY.md`
</output>
